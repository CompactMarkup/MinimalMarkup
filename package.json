{
  "name": "minimal-markup",
  "type": "module",
  "scripts": {
    "dev": "set BROWSER=google-chrome && vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest",
    "clean": "rm -rf dist/ out/",
    "purge": "rm -rf dist/ out/ node_modules/ .pnpm-debug.log"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "@tsconfig/strictest": "^2.0.5",
    "@types/node": "^20.12.12",
    "prettier": "^3.2.5",
    "tslib": "^2.6.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  },
  "browserslist": [
    "defaults"
  ],
  "packageManager": "pnpm@9.1.2"
}
